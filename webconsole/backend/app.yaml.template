runtime: python
env: flex
service: api
entrypoint: gunicorn -e INGEST_CONFIG_PATH=ingestwebconsole.{}_settings -b :$PORT main:APP

runtime_config:
  python_version: 2

skip_files:
- ^app.*yaml.*
- ^app_engine_config_generator\.py
